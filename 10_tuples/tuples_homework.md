# Задача 1

Дан список целых чисел и определенное заданное число. Найти все пары из списка,
сумма которых равна этому числу. При этом дублирования быть не должно, т.е. (4,3) и (3,4) - это одна и таже пара.
Верните из функции список кортежей.
Например: get_pairs_number([1, 2, 4, 3, 5, 2], 7) -> [(4,3), (5,2)]


# Задача 2
Дан список с информацией о сотрудниках в формате "отдел - должность - зарплата".
Напишите функцию, которая принимает такой список и возвращает новый список в формате "отдел - средняя зарплата". Т.е. функция будет вычислять среднюю зарплату по отделу.

```python
team_data = [
    ('data_analytics','Иван',400),
    ('data_analytics','Александр',380),
    ('data_analytics','Петр',410),
    ('data_analytics','Марина',350),
    ('data_engineers','Сергей',450),
    ('data_engineers','Федор',470),
    ('data_engineers','Дмитрий',420),
    ('admin_support','Василий',170),
    ('admin_support','Светлана',200),
    ('admin_support','Елена',120),
    ('admin_support','Женя',130)
]

def get_avg_salary(team_data: list[tuple[str, str, int]]) -> list[tuple[str, float]]:
    return [
        ('data_analytics',1), # средняя зп. по отделу
        ('data_engineers',2),
        ('admin_support',3),
    ]
```

