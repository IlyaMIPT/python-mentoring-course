Все задачи нужно решить с помощью списковых включений

# Задача 1.
Напишите функцию, которая принимает число n, и возвращает список четных чисел меньше n.

# Задача 2.
Дан список skills. Необходимо написать функцию, которая принимает на вход список skills, и возвращает:

```python
skills = ["python", "sql", "pandas+visualization","business cases"]
```

новый список, в котором:
1). Каждое слово с большой буквы, т.е. ["Python", "Sql", "Pandas+visualization","Business cases"]
2). Убирает символы, не относящиеся к английским буквам (органичьтесь списком таких символов +-*/@#$%^*)
3). Будут только слова, с длиной больше либо равно 6.
4). Будут только строки, состоящие их нескольких слов. Считаем, что слова разделены пробелом или спец. символом (см. п.2)
5). Будут слова из исходного списка, но все с большой буквы.
6). Будут строки из исходного списка, но сокращены с точкой, если длина строки >=3: ["pyt..", "sql", "pan...","bus..."]
7). Будут строки из исходного списка, но слова сокращены если их 2 и больше и длина слова больше либо равна 3. Например:
["python", "sql", "pan...+vis...","bus... cas..."]
Обратим внимание, что слова сокращаются если их больше двух в элементе списка, т.е. слово python сокращаться не будет.


```

